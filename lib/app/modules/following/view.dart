import 'package:flutter/material.dart';
class AllFollowingPage extends StatelessWidget {
  const AllFollowingPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
